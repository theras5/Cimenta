{
  "name": "cimenta",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev:server": "npm run dev --workspace=server",
    "start:server": "npm run start --workspace=server",
    "dev:app": "npm run start --workspace=apps/expo-app",
    "dev:web": "npm run dev --workspace=apps/web-app",
    "predev:app": "npm run build --workspace=packages/dtos"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "apps/*",
    "server",
    "bot",
    "packages/*"
  ],
  "dependencies": {
    "react-native-safe-area-context": "^5.6.1",
    "tsx": "^4.20.6"
  },
  "overrides": {
    "react": "19.1.0",
    "react-dom": "19.1.0"
  }
}
