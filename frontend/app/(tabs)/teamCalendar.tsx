import React from "react";
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const TeamCalendar = () => {
  return (
    <SafeAreaView>
      <Text>TeamCalendar</Text>
    </SafeAreaView>
  );
};

export default TeamCalendar;
